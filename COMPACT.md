# CUMCM B 题 · 常驻口令（COMPACT）

> 本文件是常驻硬约束，先于完整手册生效。细节按阶段读 `SKILL.md` 与 `references/`，不要一次灌入全部分册；两层冲突时以当前赛题为准。

## 硬约束

- 只用于全国大学生数学建模竞赛（CUMCM）本科组 B 题；不把 B 题经验机械套用于 A/C 题。
- 正文（不含附录）20–24 页，优先 21–23 页；当年官方硬性要求为最高约束。
- 摘要与关键词严格第 1 页；关键词 4–5 个，不超过 5 个。
- 不搜索、不套用优秀论文的具体模型与求解方案，避免未来透视。
- 附录与代码放主论文之后，不靠空行、巨图、重复文字凑页数。
- 优先级：当前赛题正文/附件 > 题目的数学/物理逻辑与可验证事实 > 用户给的评分细则/专家讲评/范文 > 近五年 B 题规律 > 通用算法经验。

## 算法禁令

- 问题结构决定模型，计算瓶颈决定算法；不为了显得高级而用高级算法。
- GA / PSO / SA / 蚁群 / 复杂 ML 仅在以下情形之一：目标非凸不可导或高度组合化、枚举/DP 不可行、仅作独立验证、题目本身是预测/分类且机理模型不足。
- 小规模问题不得滥用启发式算法。
- 创新只放在：重定义关键量、结构化简、模型推广、非理想修正、不确定性传播、验证设计、决策阈值或信息价值；全篇保留 1–3 个即可。

## boxed 模型

- 每问若存在目标函数、约束、状态方程或联立条件，先逐式推导，再用带大括号的统一模型块 `\boxed{ \left\{ ... \right. }` 集中收束一次，不得让关键式子散落多页。
- 只有模型非常简单时才可省略集中汇总。

## 验证底线

- 至少完成三类验证中的两类（理论 / 独立数值 / 数据），关键问题优先三类齐全。
- 至少设计一种与主求解链不同的验证方式（Monte Carlo、另一算法、手算小例、暴力枚举等）。
- 灵敏度分析 ≠ 参数 ±5%；优先找策略切换阈值、稳定区间、误差传播或鲁棒边界。
- 复杂模型必须有"基础模型失效"的证据或必要性说明。

## 页数

- 主论文正文（不含附录）：20–24 页，优先 21–23 页。
- 摘要与关键词：第 1 页；问题重述从第 2 页开始。
- 附录 + 代码页数不做上限。

## 五问终检（提交前必须能清楚回答）

1. 为什么这样定义？
2. 为什么这个模型成立？
3. 为什么选择这个算法而不是更复杂的算法？
4. 模型什么时候会失效，结果为什么可信？
5. 最终实际应该怎么做？
